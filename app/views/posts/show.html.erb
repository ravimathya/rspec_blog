<h1> Blog Posts </h1>
<h2><%= @post.title %> by <%= @post.author %> </h2>
<p>
  <%= @post.content%>
</p>
<%= link_to 'Edit Post', edit_post_path(@post.id), class: 'btn btn-secondary'%>
<%= link_to 'Delete Post', post_path(@post.id), method: :delete, class: 'btn btn-danger' %>
